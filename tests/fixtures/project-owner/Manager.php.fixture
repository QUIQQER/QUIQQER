<?php

namespace QUI\Projects;

final class Manager
{
    public static function getConfig(): self
    {
        return new self();
    }

    public function toArray(): array
    {
        return in_array($GLOBALS['argv'][3], ['cleanup', 'claim-existing'], true) ? ['phpunit' => []] : [];
    }
}
